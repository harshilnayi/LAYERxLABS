# StripSec

`StripSec` is the planned Layer 5 project in the `LAYERxLABS` series.

This folder is a placeholder for now so the repo already reflects the full project arc:

- `NetScope` for Layer 1
- `SniffCore` for Layer 2
- `StripSec` for Layer 5

When we start this one, we will give it the same treatment:

- clear scope
- clean evidence
- strong README
- measurable outcomes
